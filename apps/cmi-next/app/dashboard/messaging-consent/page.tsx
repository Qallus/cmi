import { MessagingConsentClient } from "./messaging-consent-client";

export const metadata = { title: "Messaging Consent — CMI Dashboard" };

export default function MessagingConsentPage() {
  return <MessagingConsentClient />;
}

import { RecordingStudioClient } from "./recording-studio-client";

export const metadata = { title: "Recording Studio — CMI Dashboard" };

export default function RecordingStudioPage() {
  return <RecordingStudioClient />;
}
